{
  "name": "prd-spec-verifier",
  "version": "0.6.1",
  "description": "Deterministic PRD structure verification with two MCP tools and shared audit skills.",
  "mcpServers": {
    "prd-spec-verifier": {
      "command": "node",
      "args": [
        "${extensionPath}/mcp-server/index.js",
        "--profile",
        "verifier"
      ]
    }
  }
}
